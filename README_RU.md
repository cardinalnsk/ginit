# ginit - Генератор Go проектов

[English](README.md) | [Русский](README_RU.md)

**ginit** - это инструмент командной строки для быстрого создания структурированных Go проектов с предварительно настроенными шаблонами для различных типов приложений.

## 🚀 Возможности

- **Интерактивный TUI интерфейс** - удобный текстовый пользовательский интерфейс
- **CLI режим** - создание проектов через командную строку
- **Поддержка типов проектов**:
  - **CLI** - консольные приложения
  - **Web** - веб-приложения с HTTP сервером
  - **Library** - библиотеки и пакеты
- **Автоматическая инициализация Git репозитория**
- **Предварительно настроенная структура проекта**
- **Готовые шаблоны конфигурации и логгирования**

## 📦 Установка

### Предварительные требования

- Go 1.21+
- Git (для инициализации репозитория)

### Установка из исходного кода

```bash
git clone https://github.com/your-username/ginit.git
cd ginit
go build -o ginit ./cmd/ginit
sudo mv ginit /usr/local/bin/
```

### Установка через go install

```bash
go install github.com/your-username/ginit/cmd/ginit@latest
```

## 🎯 Использование

### Интерактивный режим (TUI)

```bash
ginit
```

Интерактивный интерфейс проведет вас через процесс создания проекта:

1. **Название проекта** - имя вашего проекта
2. **Имя модуля** - Go module name (например: github.com/user/project)
3. **Директория** - путь для создания проекта
4. **Тип проекта** - CLI, Web или Library
5. **Инициализация Git** - создание Git репозитория

### Командная строка (CLI)

```bash
# Создание CLI проекта
ginit -name my-cli-app -module github.com/user/my-cli-app -dir ./my-cli-app -type cli

# Создание Web проекта
ginit -name my-web-app -module github.com/user/my-web-app -dir ./my-web-app -type web

# Создание Library проекта
ginit -name my-lib -module github.com/user/my-lib -dir ./my-lib -type library
```

#### Параметры командной строки

- `-name` - название проекта (обязательно)
- `-module` - имя Go модуля (обязательно)
- `-dir` - директория для создания проекта (обязательно)
- `-type` - тип проекта: cli, web, library (обязательно)
- `-vcs` - инициализировать Git репозиторий (true/false, по умолчанию: true)

## 🏗️ Структура проекта

### CLI проект

```
my-cli-app/
├── cmd/
│   └── my-cli-app/
│       └── main.go
├── internal/
│   ├── app/
│   │   └── app.go
│   ├── cli/
│   │   ├── cli.go
│   │   ├── commands.go
│   │   └── config.go
│   └── config/
│       └── config.go
├── pkg/
│   └── logger/
│       └── logger.go
├── go.mod
├── go.sum
└── README.md
```

### Web проект

```
my-web-app/
├── cmd/
│   └── web/
│       └── main.go
├── internal/
│   ├── app/
│   │   └── app.go
│   ├── config/
│   │   └── config.go
│   ├── handlers/
│   │   └── handlers.go
│   └── server/
│       └── server.go
├── pkg/
│   └── logger/
│       └── logger.go
├── static/
├── templates/
├── go.mod
├── go.sum
└── README.md
```

### Library проект

```
my-lib/
├── internal/
│   └── config/
│       └── config.go
├── pkg/
│   └── mylib/
│       └── mylib.go
├── go.mod
├── go.sum
└── README.md
```

## 🔧 Генерируемые файлы

### Основные файлы

- **main.go** - точка входа приложения
- **go.mod** - файл модуля Go
- **README.md** - документация проекта
- **.gitignore** - игнорируемые файлы Git

### Конфигурация

- **config.go** - конфигурация приложения с поддержкой env переменных
- Система singleton для доступа к конфигурации
- Поддержка различных типов конфигурации для разных типов проектов

### Логгирование

- **logger.go** - утилиты логгирования на основе slog
- Поддержка текстового и JSON формата
- Настраиваемые уровни логирования

## 🎨 TUI интерфейс

Интерактивный интерфейс предоставляет:

- **Пошаговый мастер** создания проекта
- **Валидация ввода** - проверка корректности данных
- **Предварительный просмотр** структуры проекта
- **Клавиши навигации**:
  - `Enter` - следующий шаг
  - `Tab` / `Shift+Tab` - переключение между полями
  - `Ctrl+C` - выход
  - `h`/`l` или стрелки - выбор типа проекта
  - `y`/`n` - выбор инициализации Git

## 🛠️ Разработка

### Сборка проекта

```bash
# Сборка ginit
go build -o ginit ./cmd/ginit

# Тестирование
go test ./...
```

### Структура кода ginit

```
ginit/
├── cmd/
│   └── ginit/
│       └── main.go          # Точка входа
├── internal/
│   ├── generator/
│   │   ├── generator.go     # Логика генерации проектов
│   │   ├── templates.go     # Шаблоны файлов
│   │   └── utils.go         # Вспомогательные функции
│   └── tui/
│       ├── model.go         # Модель TUI
│       └── styles.go        # Стили интерфейса
├── go.mod
└── README.md
```

## 🐛 Устранение неполадок

### Проблемы с созданием проекта

**Проблема**: Проект создается не полностью
**Решение**: Убедитесь, что вы не нажимаете Enter несколько раз во время создания проекта

**Проблема**: Ошибка "path not in whitelist"
**Решение**: Используйте абсолютные пути или пути в домашней директории

### Проблемы с зависимостями

**Проблема**: Ошибки импорта после создания проекта
**Решение**: Выполните `go mod tidy` в созданной директории проекта

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Распространяется под лицензией MIT. Смотрите файл `LICENSE` для подробностей.

## 📞 Контакты

- GitHub: [your-username](https://github.com/your-username)
- Email: your-email@example.com

---

⭐ Если этот проект был полезен, поставьте звезду на GitHub!